<%= form_with(model: payment_mentor) do |form| %>
  <% if payment_mentor.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(payment_mentor.errors.count, "error") %> prohibited this payment_mentor from being saved:</h2>

      <ul>
        <% payment_mentor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :payment_proof, style: "display: block" %>
    <%= form.text_field :payment_proof %>
  </div>

  <div>
    <%= form.label :is_valid_payment, style: "display: block" %>
    <%= form.check_box :is_valid_payment %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
